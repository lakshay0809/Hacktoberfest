#include <stdio.h>
#include <math.h>
#include <stdlib.h>


int bitAND(int a, int b)
{
    return ~((~a) | (~b));
}

int main()
{
    int x, y;
    scanf("%d %d", &x, &y);
    printf("%d\n", bitAND(x, y));
    return 0;
}
